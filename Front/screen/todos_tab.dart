import 'package:flutter/material.dart';

class TodosTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Todos Screen', style: TextStyle(fontSize: 24.0)),
      ),
    );
  }
}
